{% extends "base.html.twig" %}

{% block title %}All my Recipes
{% endblock %}

{% block body %}
	<h1>Cuisinons Ensemble !</h1>
	<a class="btn btn-primary m-auto" href={{path('recipe_create')}}>Créer</a>
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Titre</th>
				<th>Action</th>
			</tr>
		</thead>
		<tbody>
			{% for recipe in recipes %}
				<tr>
					<td>
						<a href={{path('recipe_show', {id: recipe.id, slug: recipe.slug} )}}>{{recipe.title}}</a>
					</td>
					<td>
						<a class="btn btn-warning" href={{path('recipe_edit', {id: recipe.id} )}}>Editer</a>
					</td>
					{# <td>
													<a href={{path('recipe_show', {id: recipe.id} )}}>Supprimer</a>
													</td> #}
				</tr>

			{% endfor %}
		</tbody>
	</table>
	<ul></ul>
{% endblock %}
